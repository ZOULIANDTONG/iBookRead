# iBookRead

<div align="center">

📚 一个轻量级、功能强大的命令行文档阅读工具

[![Python Version](https://img.shields.io/badge/python-3.8+-blue.svg)](https://www.python.org/downloads/)
[![Tests](https://img.shields.io/badge/tests-178%20passed-brightgreen.svg)](tests/)
[![Coverage](https://img.shields.io/badge/coverage-71%25-yellow.svg)](tests/)

支持 EPUB、MOBI、TXT、Markdown 等多种文档格式

</div>

---

## ✨ 特性

- 📖 **多格式支持** - 支持 EPUB、MOBI、TXT、Markdown 等常见文档格式
- 🎨 **美观界面** - 基于 Rich 库的现代化终端 UI
- 🔖 **书签管理** - 添加、查看、删除书签，快速定位阅读位置
- 💾 **进度保存** - 自动保存阅读进度，下次打开继续阅读
- 🔒 **密码保护** - 可选的密码保护功能，保护隐私
- 🌏 **中英文支持** - 完美支持中英文混排显示
- ⚡ **快捷键操作** - Vim 风格的快捷键，高效阅读
- 🖥️ **跨平台** - 支持 Linux、macOS、Windows

---

## 📦 安装

### 方式一：使用 pip 安装（推荐）

```bash
pip install ibook-reader
```

### 方式二：从源码安装

```bash
# 克隆仓库
git clone https://github.com/yourusername/iBookRead.git
cd iBookRead

# 安装
pip install .
```

### 方式三：使用 wheel 包安装

```bash
pip install ibook_reader-1.0.0-py3-none-any.whl
```

---

## 🚀 快速开始

### 基本用法

```bash
# 默认模式：直接输出全文（纯文本模式）
ibook your_book.epub

# 使用交互式UI模式
ibook --ui your_book.epub

# 打开 MOBI 文件
ibook novel.mobi

# 打开 TXT 文件
ibook document.txt

# 打开 Markdown 文档
ibook README.md
```

### 首次使用

**默认模式（纯文本输出）**：

```bash
$ ibook my_book.epub
正在加载文档: my_book.epub...
✓ 文档加载成功
标题: My Book
作者: Author Name
章节数: 15

===============================================

第一章

内容直接输出...
```

**UI模式（需要添加 --ui 参数）**：

首次运行UI模式时，程序会提示设置密码（可选）：

```bash
$ ibook --ui my_book.epub
请设置密码（留空跳过）: ****
请再次输入密码: ****
✓ 密码设置成功

正在加载文档: my_book.epub...
✓ 文档加载成功
  标题: My Book
  章节数: 15
  总页数: 234

按 ? 键查看帮助信息...
```

---

## ⌨️ 快捷键

**注意**：快捷键仅在 `--ui` 模式下有效。默认的纯文本模式会直接输出全文。

### 导航操作

| 按键 | 功能 | 说明 |
|-----|------|------|
| `↓` / `j` / `Space` | 下一页 | 向下翻一页 |
| `↑` / `k` | 上一页 | 向上翻一页 |
| `→` / `l` | 下一章 | 跳到下一章节 |
| `←` / `h` | 上一章 | 跳到上一章节 |
| `g` | 跳到开头 | 跳转到文档开头 |
| `G` | 跳到结尾 | 跳转到文档结尾 |

### 书签操作

| 按键 | 功能 | 说明 |
|-----|------|------|
| `m` | 添加书签 | 在当前位置添加书签 |
| `b` | 书签列表 | 查看所有书签（开发中） |

### 其他操作

| 按键 | 功能 | 说明 |
|-----|------|------|
| `?` | 帮助 | 显示快捷键帮助 |
| `q` / `ESC` | 退出 | 保存进度并退出 |

---

## 🔧 命令行选项

```bash
ibook [选项] [文件路径]
```

### 选项说明

| 选项 | 说明 |
|-----|------|
| `-h`, `--help` | 显示帮助信息 |
| `--version` | 显示版本号 |
| `--ui` | 使用交互式UI模式（默认为纯文本模式） |
| `--page N` | 跳转到指定页码（仅UI模式） |
| `--chapter N` | 跳转到指定章节（仅UI模式） |
| `--percent N` | 跳转到指定百分比进度，0-100（仅UI模式） |
| `--clean` | 清理所有数据（配置、进度、书签） |
| `--reset-password` | 重置密码 |

### 使用示例

```bash
# 查看版本
ibook --version

# 查看帮助
ibook --help

# 使用UI模式读取文档
ibook --ui book.epub

# 从第10页开始阅读
ibook --ui --page 10 book.epub

# 从第5章开始阅读
ibook --ui --chapter 5 book.epub

# 从50%进度开始阅读
ibook --ui --percent 50 book.epub

# 重置密码
ibook --reset-password

# 清理所有数据
ibook --clean
```

---

## 📖 界面说明

### 阅读界面布局

```
┌─────────────────────────────────────────────────────┐
│  红楼梦.epub | 第3章/共120章 | 第45页/共523页 | 8.6%  │  ← 标题栏
├─────────────────────────────────────────────────────┤
│                                                     │
│  第三回 贾雨村夤缘复旧职 林黛玉抛父进京都            │
│                                                     │
│      话说雨村忙回头看时，不是别人，乃是当日同       │
│  僚一案参革的张如圭。他系此地人，革后家居，         │
│  今打听得都中奏准起复旧员之信，他便四下里寻         │  ← 内容区
│  情找门路，忽遇见雨村，故忙道喜。二人见了礼，       │
│  张如圭便将此信告诉雨村...                         │
│                                                     │
│                                                     │
├─────────────────────────────────────────────────────┤
│ [=====>              ] 8.6% | ↑↓翻页 ←→切章 ?帮助  │  ← 状态栏
└─────────────────────────────────────────────────────┘
```

### 界面元素说明

- **标题栏**：显示文档名、当前章节、页码、阅读进度
- **内容区**：显示当前页的文本内容
- **状态栏**：显示进度条和常用快捷键提示

---

## 📁 数据存储

所有配置和数据存储在用户目录下：

```
~/.ibook_reader/
├── config.json          # 配置文件（密码哈希等）
├── progress.json        # 阅读进度
└── bookmarks/           # 书签目录
    ├── abc123def456.json
    └── ...
```

- **Linux/macOS**: `~/.ibook_reader/`
- **Windows**: `%USERPROFILE%\.ibook_reader\`

---

## 🔒 安全性

### 密码保护

iBookRead 使用 **SHA-256** 算法加密存储密码：

- 密码经过哈希处理，不以明文存储
- 每个密码都有唯一的随机盐值
- 密码验证失败3次后自动退出
- 支持随时重置密码

### 隐私保护

- 所有数据都存储在本地
- 不会收集或上传任何用户数据
- 可随时使用 `--clean` 清理所有数据

---

## 🎯 支持的文件格式

| 格式 | 扩展名 | 支持程度 | 说明 |
|-----|-------|---------|------|
| **EPUB** | `.epub` | ✅ 完全支持 | 提取文本内容，支持章节导航 |
| **MOBI** | `.mobi`, `.azw` | ✅ 完全支持 | 提取文本内容 |
| **TXT** | `.txt` | ✅ 完全支持 | 自动检测编码（UTF-8/GBK/GB2312等） |
| **Markdown** | `.md`, `.markdown` | ✅ 完全支持 | 按一级标题分章节 |

---

## 🛠️ 开发

### 环境要求

- Python 3.8 或更高版本
- pip

### 安装开发依赖

```bash
# 克隆仓库
git clone https://github.com/yourusername/iBookRead.git
cd iBookRead

# 创建虚拟环境
python -m venv venv
source venv/bin/activate  # Linux/macOS
# 或
venv\Scripts\activate     # Windows

# 安装开发依赖
pip install -e .
pip install pytest pytest-cov
```

### 运行测试

```bash
# 运行所有测试
pytest

# 运行测试并查看覆盖率
pytest --cov=ibook_reader --cov-report=term

# 运行特定测试
pytest tests/unit/test_parsers.py
```

### 构建发布包

```bash
# 安装构建工具
pip install build

# 构建
python -m build

# 生成的包在 dist/ 目录
ls dist/
# ibook_reader-1.0.0-py3-none-any.whl
# ibook_reader-1.0.0.tar.gz
```

---

## 📋 技术栈

- **语言**: Python 3.8+
- **UI框架**: [Rich](https://github.com/Textualize/rich) - 现代化终端UI
- **EPUB解析**: [ebooklib](https://github.com/aerkalov/ebooklib)
- **MOBI解析**: [mobi](https://github.com/iscc/mobi)
- **Markdown解析**: [markdown](https://github.com/Python-Markdown/markdown)
- **编码检测**: [chardet](https://github.com/chardet/chardet)
- **HTML解析**: [BeautifulSoup4](https://www.crummy.com/software/BeautifulSoup/)
- **测试框架**: [pytest](https://pytest.org/)

---

## 🗺️ 项目结构

```
iBookRead/
├── ibook_reader/           # 主包目录
│   ├── __init__.py
│   ├── cli.py             # 命令行入口
│   ├── config.py          # 配置管理
│   ├── models/            # 数据模型
│   ├── parsers/           # 文档解析器
│   ├── core/              # 核心功能（分页引擎）
│   ├── services/          # 业务服务层
│   ├── ui/                # 用户界面
│   └── utils/             # 工具函数
├── tests/                 # 测试目录
│   ├── unit/             # 单元测试
│   └── integration/      # 集成测试
├── dist/                  # 发布包目录
├── setup.py              # 安装配置
├── pyproject.toml        # 项目配置
├── requirements.txt      # 依赖清单
└── README.md            # 本文件
```

---

## 🤝 贡献

欢迎贡献代码！请遵循以下步骤：

1. Fork 本仓库
2. 创建您的特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交您的更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 打开一个 Pull Request

### 代码规范

- 遵循 PEP 8 代码风格
- 添加必要的注释和文档字符串
- 确保所有测试通过
- 保持测试覆盖率在 70% 以上

---

## 📝 更新日志

### v1.0.0 (2024-11-13)

**首次发布** 🎉

- ✅ 支持 EPUB、MOBI、TXT、Markdown 格式
- ✅ 完整的阅读功能（翻页、跳转、章节导航）
- ✅ 书签管理
- ✅ 进度自动保存
- ✅ 密码保护
- ✅ 美观的终端 UI
- ✅ 跨平台支持
- ✅ 178 个测试用例，71% 代码覆盖率

---

## 📄 许可证

本项目采用 [MIT License](LICENSE) 许可证。

---

## 🙏 致谢

- [Rich](https://github.com/Textualize/rich) - 提供美观的终端UI框架
- [ebooklib](https://github.com/aerkalov/ebooklib) - EPUB解析支持
- [mobi](https://github.com/iscc/mobi) - MOBI格式支持
- 所有贡献者和用户


---

## 🔮 未来计划

- [ ] 搜索功能（在文档中搜索关键词）
- [ ] 主题切换（暗色/亮色/护眼主题）
- [ ] 字体大小调整
- [ ] PDF 格式支持
- [ ] 笔记功能
- [ ] 目录导航
- [ ] 云同步支持

---

<div align="center">

**如果觉得有用，请给个 ⭐️ 支持一下！**

Made with ❤️ by iBookRead Team

</div>
